streamlit>=1.50,<2
pandas>=2.0
numpy>=1.24
scipy>=1.11
pyarrow>=12.0
requests>=2.31
matplotlib>=3.7
# Interactive app charts. Keep the major version bounded for deployment stability.
plotly>=5.24,<7
# App (deploy) dependencies only. The pinned build-only sentiment scorer used by
# scripts/run_part_b.py lives in requirements-dev.txt. Install both files for a
# clean-machine reproduction; keep the deployed app on requirements.txt so cold
# starts stay fast (precompute sentiment and load the result, do not score it there).
