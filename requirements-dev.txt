# Install with requirements.txt for analytical reproduction, report artifacts,
# and tests. These packages are not imported by the deployed app.
scipy>=1.11
pyarrow>=12.0
requests>=2.31
matplotlib>=3.7
vaderSentiment==3.3.2
pytest>=8,<10
